/**
 * Contact page component that displays a centered "Contact Page" heading.
 *
 * @returns A React element containing a full-height, center-aligned container with an `h1` displaying "Contact Page".
 */
export default function ContactPage() {
  return (
    <div className="flex-1 flex items-center justify-center">
      <h1 className="text-6xl">Contact Page</h1>
    </div>
  );
}