/**
 * Renders a centered page displaying a main "Features Page" heading.
 *
 * @returns A JSX element containing a full-height, centered container with an H1 labeled "Features Page".
 */
export default function FeaturesPage() {
  return (
    <div className="flex-1 flex items-center justify-center">
      <h1 className="text-6xl">Features Page</h1>
    </div>
  );
}